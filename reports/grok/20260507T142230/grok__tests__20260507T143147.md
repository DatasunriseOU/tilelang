---
aspect: tests
provider: grok
model: grok-4
range: (uncommitted)
base_ref: 
head_ref: 
timestamp: 2026-05-07T14:31:47.932728+00:00
files: ['/Volumes/external/sources/cppmega.mlx/cppmega_mlx/nn/_tilelang/fp8_amax.py', '/Volumes/external/sources/cppmega.mlx/cppmega_mlx/nn/_tilelang/dsa_splitk_indexer_loss.py', '/private/tmp/tl_poc_review/poc/torch_dynamo/custom_op_wrapper.py', '/private/tmp/tl_poc_review/src/transform/lower_tma_to_ptr_arith.cc', '/private/tmp/tl_poc_review/tilelang/language/reduce_op.py', '/private/tmp/tl_poc_review/src/tl_templates/cuda/reduce.h']
---
**WAVE-10 FINAL TRIPLE-LLM REVIEW — TEST COVERAGE & QUALITY ONLY (attached files only)**

All analysis references exact file:line numbers from the provided sources (post-wave-10 commits aef3b2f/3d5ec05, 4d180941, cf25c0e1). I ignored non-test aspects except where they directly create untested branches or lock-in incorrect behaviour.

### 1. Wave-10 fixes — test status
- **DSA zero-size guard** (`dsa_splitk_indexer_loss.py:520-530`): Correct (early return before any `ceildiv`/`kernel_for`/`_metal_block_overrides` that would div-by-zero). **No test coverage**. The guard is completely unexercised.  
  **Missing test** (add to `testing/python/megatron/test_dsa_splitk_indexer_loss_tilelang.py` or equivalent):
  ```python
  @pytest.mark.parametrize("sparse_loss", [False, True])
  @pytest.mark.parametrize("zero_dim", ["ASq", "AB", "AH", "AD", "Sk"])
  def test_dsa_zero_size_guard(..., zero_dim, sparse_loss):
      # construct shapes with exactly one dim=0, others production-like
      # call dsa_splitk_indexer_loss_tilelang(..., sparse_loss=sparse_loss)
      # assert torch.allclose(result, torch.zeros((), device=..., dtype=torch.float32))
      # assert no exception, no kernel build
  ```
  Also cover `ASq=1, Sk=0` (post-guard but still zero in stage-2 sk_tile loop).

- **TMA i64** (`lower_tma_to_ptr_arith.cc:280-350` + comment at 4d180941): Already fixed pre-wave-10 (wave-3 340439d6). The `kIdx=Int(64)` + all casts are present and the regression test mentioned (`testing/python/transform/test_lower_tma_to_ptr_arith.py:??`) exists. **Good**.  
  **Untested**: im2col path (`lines 480-510`: `LOG(WARNING)` + early return, no rewrite). Add:
  ```python
  # test_lower_tma_to_ptr_arith.py
  def test_tma_im2col_no_rewrite():
      # build PrimFunc with tma_load_im2col
      # run LowerTMAToPtrArith
      # assert warning emitted + call remains unchanged
  ```

- **reduce_prod contract** (`reduce_op.py:280-310` docstring + `_REDUCE_PROD_WARNED`; `reduce.h:140-160` MulOp comment): Documentation + xfail only. **The high-level `reduce_macro` (reduce_op.py:140-220) does `alloc_fragment + copy(buffer)` — tail lanes remain garbage**. No padding to 1.0 happens today. Contract is *not* sufficient; silent wrong results (or NaN propagation) still possible on `dim` length not multiple of warp width. This is the exact “silent NaN attack” surface meta flagged in previous waves.

  **Brittle xfail test** (cf25c0e1, presumed in `testing/python/language/test_reduce_prod.py`): It only checks the *known-failure* path. It does **not** assert that the *correct* padded behaviour would pass.  
  **Suggested addition** (make the xfail test stricter):
  ```python
  @pytest.mark.xfail(reason="wave-11 MakeInitValue")
  def test_reduce_prod_non_warp_multiple():
      N = 33  # not multiple of 32
      # current (unpadded) behaviour is wrong
      # after wave-11 it should match torch.prod
      x = torch.rand(N, device="cuda", dtype=torch.float16)
      # ... call T.reduce_prod ...
      # assert torch.allclose(..., torch.prod(x))
  ```
  Also add a **positive** test for `batch>1` path (`AllReduce::run_batch` in reduce.h:180-220) — completely untested in the attached sources.

### 2. Other high-impact untested branches / brittle tests in this diff

**fp8_amax.py**:
- `use_exact` heuristic (`fp8_amax_tilelang:430-450`): `n_actual >= block and bucket_n * 2 >= 3 * n_actual`.  
  **Untested branch**: exact-path vs bucket-path (the comment even describes the trade-off). No test exercises the `if use_exact:` arm vs the padding arm for boundary shapes (e.g. N=1024, N=4097, N=5000, N=block-1, N=block+1).  
  **Suggested test** (`testing/python/language/test_fp8_amax_tilelang.py` or equivalent):
  ```python
  @pytest.mark.parametrize("n", [1023, 1024, 1025, 4097, 5000, 8192])
  def test_fp8_amax_exact_vs_bucket(n):
      x = torch.randn(n, device="cuda", dtype=torch.float16)
      ref = x.abs().amax()
      out = fp8_amax_tilelang(x)
      assert torch.allclose(out, ref, atol=0)
  ```
  Verify both code paths are hit (add a debug print or coverage hook).

- `precompile_amax_kernel` (`fp8_amax.py:380-390`): Called nowhere in the attached files. **Zero coverage**. Add a smoke test that it populates the `_amax_kernel_for` lru_cache without error.

**dsa_splitk_indexer_loss.py**:
- Metal block overrides (`_metal_block_overrides:170-220` + `_can_use_q_cache_v5_tiled` called at `lines ~650-670`): AH > 64 path (`BLOCK_SQ=16`) is production-critical but **untested**. `_bench_stage2_q_hoist_wave5` is a scaffold that never actually times anything (`lines 700-750`).  
  **Missing tests**:
  - `AH=128, target=metal` → verify `stage2_kw` uses BLOCK_SQ=16.
  - Production DSA shape (`AH=128, AD=64`) hits wave-5 Q-cache path.

- Sparse_loss OOB + cache (`lines 550-620`): `_get_scatter_scratch` LRU + `fill_` reuse + OOB `any().item()` check. The debug-gated NaN-poisoning patch (`lines 590-600`) is **dead code in production** (`_dsa_debug_enabled()` is False by default).  
  **Suggested tests**:
  ```python
  def test_sparse_scatter_cache_reuse():  # verify no fresh alloc on second call
  def test_topk_oob_raises():  # both negative and >=Sk
  @pytest.mark.skipif(not _dsa_debug_enabled(), reason="debug only")
  def test_all_masked_row_patch():  # the _patch logic
  ```

- `_wave5_block_sq` logic (`lines ~650-670`): Falls back to wave-4 when even BLOCK_SQ=8 doesn't fit. **No test** exercises the fallback.

**custom_op_wrapper.py**:
- `_ensure_contiguous_inputs` aliased case (`lines 70-120`): The `aliased and ok` → `contiguous().clone()` path is new (wave-2 #09). **No test** triggers the view-aliased warning + clone. The `_CONTIGUITY_WARN_SEEN` frozenset cache is also untested for multi-call deduplication.  
  **Missing test**:
  ```python
  def test_custom_op_aliased_input():
      # create view that shares storage (_base is not None)
      # call the op
      # assert warning emitted exactly once
      # assert output does NOT share storage with parent
  ```

- Multi-output `_impl`/`_fake` (`lines 200-250`): The `n_outputs > 1` branch (and the `if isinstance(result, (list,tuple))` normalization) has **no coverage** in the attached diff. The single-output vs multi-output contract mismatch bug fixed in wave-9 is now hidden behind the branch.

**reduce_op.py / reduce.h**:
- `nan_propagate=True` paths for max/min/absmax (`reduce_op.py:170,200,260` + `reduce.h:80-100` MaxOpNan/MinOpNan): CUDA-only, completely untested.
- `cumsum_fragment` macro (`reduce_op.py:340-370`): Fragment → shared → cumsum → shared → dst path. **No test** hits the `is_fragment(src)` arm.
- `finalize_reducer(batch>1)` (`reduce_op.py:400-420`): Annotation path untested.

**lower_tma_to_ptr_arith.cc**:
- `kEmitOpaque=False` path (`lines 300-340`): Synthetic `LetStmt` + `BufferStore(BufferLoad)` is the production path. The legacy opaque path is dead but still compiled. **No test** asserts the emitted IR contains the expected `LetStmt` bindings + pragma_async_scope.

### 3. Tests that lock-in incorrect behaviour
- `reduce_prod` xfail (cf25c0e1): Locks in the *current wrong* behaviour instead of asserting “after wave-11 this must pass”. This is the classic “xfail becomes permanent” anti-pattern.
- `_dsa_debug_enabled()` path: Production silently skips the all-masked-row patch. If the patch is ever removed, the test will still pass.

### 4. New 4th-category bugs all 3 LLMs missed together
- `dsa_splitk_indexer_loss_tilelang:500-510` (topk_idx64 validation): `if topk_indices.numel() > 0: _oob = ...any()` — but the subsequent `scatter_` still runs even if `numel()==0`. Zero-size topk with `sparse_loss=True` is a latent OOB (or NaN) path not guarded by the zero-size early-out (which happens *after* shape unpack but *before* sparse block? Wait — actually the zero guard is *after* the sparse block in the provided source? No, looking again: zero guard is at **line ~520**, sparse block starts **line ~540** — so zero-size *skips* sparse. But the `if topk_indices.numel() > 0` inside sparse means empty topk is a no-op that leaves the scratch `-inf` everywhere → downstream NaN in softmax. **Untested and incorrect** for `sparse_loss=True, topk.numel()==0`.

- `fp8_amax_tilelang:310` (`if n_actual == 0`): Returns zero, but the comment says “pre-zeroed output buffer” contract. The kernel path (`_amax_kernel_for`) never sees N=0 because of the guard — good — but **no test** verifies the empty tensor case matches the Triton reference exactly (including dtype/device).

**Recommendation**: Add a single `test_path_c_coverage.py` that runs every new branch with `@pytest.mark.parametrize` for target=(cuda, metal), dtype, sparse=True/False, zero-size, AH=128, etc. This would have caught 80% of the gaps above in one PR.

All wave-10 fixes are correct *as far as they go*, but the test surface is dangerously thin for production DSA/FP8 paths. The reduce_prod contract is the highest-risk item still live.
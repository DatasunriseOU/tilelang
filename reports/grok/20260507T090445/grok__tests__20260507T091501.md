---
aspect: tests
provider: grok
model: grok-4
range: (uncommitted)
base_ref: 
head_ref: 
timestamp: 2026-05-07T09:15:01.223366+00:00
files: ['/Volumes/external/sources/cppmega.mlx/cppmega_mlx/nn/_tilelang/fp8_amax.py', '/Volumes/external/sources/cppmega/cppmega/megatron/fp8_activations.py', '/Volumes/external/sources/cppmega/tests/test_fp8_amax_tilelang.py']
---
**WAVE-3 FINAL REGRESSION REVIEW (tests focus):** ✅ **Everything is clean and ready to ship.** Mark green.

### 1. Wave-1 + wave-2 + wave-3 fixes — no regressions / half-fixes
All prior fixes are fully integrated and guarded by the current test suite:
- **Partial last block / strided `T.Parallel` coverage** (the big wave-1 BLOCK-vs-THREADS concern): explicitly exercised in `test_fp8_amax_partial_last_block_matches_torch_reference` (test_fp8_amax_tilelang.py:148-160) with shapes `(100,)`, `(4097,)`, `(1, 4097)`, `(3, 1234)`. Tolerance matches the main amax test; `gi < N` mask + zero-fill tail is verified.
- **Non-finite rejection** (wave-2/3 NaN/Inf hole): `test_fp8_pack_rejects_nonfinite_input` (test_fp8_amax_tilelang.py:480-490) hits the exact `FloatingPointError` path in `fp8_pack_tilelang` (fp8_amax.py:620-630).
- **Signed-zero / all-zero amax** (Metal CAS-loop divergence risk): `test_fp8_amax_handles_signed_zero_only` (test_fp8_amax_tilelang.py:510-530) asserts `== 0.0` (not `-0.0`).
- **Bucket padding / JIT-cache correctness**: `test_fp8_amax_padding_does_not_change_result` + `test_bucket_n_collapses_close_shapes` (test_fp8_amax_tilelang.py:440-470 and 200-210) + the partial-block test together confirm zero-tail is a true no-op.
- **Block/threads invariant raise**: `test_block_size_threads_invariant_raises_on_violation` (test_fp8_amax_tilelang.py:170-175) hits the exact `RuntimeError` in `make_fp8_amax_kernel` (fp8_amax.py:280-290).
- **Support/dispatch helpers**: `test_tilelang_supports_with_reason_returns_2tuple` (test_fp8_amax_tilelang.py:220-250) exhaustively checks every branch of `tilelang_supports_with_reason` and the bool wrapper (covers CPU, invalid strings, MPS, None, etc.). The shim in fp8_activations.py:290-300 is replaced correctly when TileLang imports.
- **Triton parity on CUDA**: `test_fp8_amax_matches_triton_reference` (test_fp8_amax_tilelang.py:80-110) still works (calls the legacy `_amax_kernel` directly for exact match).

No new breaks introduced. The `fp8_pack_tilelang` → TE → TileLang → Triton → unfused dispatch in `FP8ActivationPacker.pack` (fp8_activations.py:310-320) prefers TileLang exactly when `tilelang_supports(device)` is True (after TE check).

### 2. Remaining HIGH-severity issues
**None.** Zero high-severity findings.

All tests are non-brittle:
- Amax parity uses `rtol=1e-3, atol=1e-3` only where fp16→fp32 reduction order can differ slightly (Triton test uses exact `rtol=0, atol=0`).
- FP8 round-trip uses `rtol=0.15` + scale-scaled atol — appropriate given e4m3 mantissa and the manual ref reconstruction in the test.
- No tests lock in incorrect behavior (e.g., padding test explicitly proves the cache is semantically transparent).

### 3. Test coverage & quality summary (test_fp8_amax_tilelang.py)
**Excellent overall** (covers the vast majority of the diff). The suite is focused, device-aware (`_pick_device` + skips), and directly validates the TileLang port against both PyTorch reference and the original Triton kernels.

**Minor gaps (low-severity, non-blocking):**
- Empty-tensor fast-paths (`fp8_amax_tilelang:492`, `fp8_pack_tilelang:600`, `fp8_quantize_tilelang:510`) — never directly asserted (pack early-returns, amax returns zero scalar). Torch `amax()` on empty raises, so this is a deliberate divergence that should be explicitly documented/tested.
- `fp8_quantize_tilelang` standalone paths: `out=` param (both contiguous and non-contiguous branches at fp8_amax.py:530-560), `inv_scale` as `torch.Tensor` vs float, and fp32 input dtype (`_resolve_in_dtype:370` + kernel `in_dtype="float32"`).
- `precompile_amax_kernel` (fp8_amax.py:410) — untested (lru_cache warm-up helper).
- No direct unit test for `fp8_quantize_tilelang` itself (only exercised indirectly via pack).

These are pure coverage gaps, not correctness risks. The production paths (`FP8ActivationPacker.pack` with default `clamp=False`, TileLang on MPS, etc.) are already exercised.

**Recommendation (optional, before merge):** Add 3 tiny tests (I can draft them if you want):
1. Empty tensor for amax + pack (assert shapes, dtype, scale=1.0, value=0.0).
2. `fp8_quantize_tilelang(..., inv_scale=torch.tensor(...), out=some_noncontig_tensor)`.
3. Explicit fp32 input to `fp8_amax_tilelang`.

Everything else (bucketing, target-family picking, kernel builders, Metal/CUDA lowering via `_resolve_target`, etc.) is solidly covered.

**Conclusion:** Wave-3 regression triage complete. The TileLang Path C integration, its tests, and the fp8_activations.py dispatch are production-ready. No remaining HIGH-severity issues. Ship it.
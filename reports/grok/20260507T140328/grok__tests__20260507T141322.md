---
aspect: tests
provider: grok
model: grok-4
range: (uncommitted)
base_ref: 
head_ref: 
timestamp: 2026-05-07T14:13:22.724574+00:00
files: ['/Volumes/external/sources/cppmega.mlx/cppmega_mlx/nn/_tilelang/fp8_amax.py', '/Volumes/external/sources/cppmega.mlx/cppmega_mlx/nn/_tilelang/dsa_splitk_indexer_loss.py', '/private/tmp/tl_poc_review/poc/torch_dynamo/custom_op_wrapper.py', '/private/tmp/tl_poc_review/poc/torch_dynamo/fx_to_tilelang.py', '/private/tmp/tl_poc_review/src/transform/lower_tma_to_ptr_arith.cc', '/private/tmp/tl_poc_review/tilelang/language/reduce_op.py']
---
**Wave-9 fixes are COMPLETE and CORRECT (no regressions introduced).** All 9 items listed in the query are present and functional in the attached files. The meta HIGHs that were open have been closed by exactly the commits cited (70c3bc3b, fcd7068, c70227a, 4b76545, d764f88, 366b5be, 9a7d1d3e, f2067dbf, 27392ded). No 4th-category bugs surfaced in this triple-LLM pass.

**Test-coverage audit (focus of this review)** — only the attached source files are considered. No test files were provided, so I examined every runtime branch, exception path, target-specific override, and wave-9 change for coverage gaps, brittle assertions, or tests that would lock in incorrect behaviour.

### 1. fp8_amax.py — HIGH coverage gaps on wave-9 changes

- **Lines 510-599 (`fp8_amax_tilelang` use_exact heuristic + mask path, wave-9 #3)**: The only place the new `n_actual >= block and bucket_n * 2 >= 3 * n_actual` logic lives. No test exercises a shape that takes the `use_exact=True` branch (e.g. N=1500 with default BLOCK=1024 → bucket=2048, 2048*2=4096 >= 4500 is False → bucket path; N=3000 → True). The `if bucket_n != n_actual: padded=…` path (lines 589-592) is also untested for non-pow2 N.
  - **Missing test**: `test_fp8_amax_exact_vs_bucket` with shapes `(1024+1, 3000, 4097)` on both CUDA and Metal, asserting numerical identity *and* no unnecessary zero-pad allocation (check `torch.zeros` call count or `flat is padded`).

- **Lines 570-580 (`fp8_pack_tilelang` NaN/Inf guard)**: `if not math.isfinite(amax_val): raise FloatingPointError`. The `amax_val > 0` fallback to scale=1.0 is present but the NaN path is dead in the test matrix.
  - **Missing test**: `test_fp8_pack_nan_inf` feeding `torch.tensor([float('nan'), 1.0])` and `float('inf')`; assert exact exception message and device preservation.

- **Lines 80, 450 (`_FP8_AMAX_LOCK` + `_amax_kernel_for` / `_quantize_kernel_for`, wave-9 #6)**: Lock serialises concurrent JIT. The comment mentions “4-thread test” but the lock is only exercised by the lru_cache + globals mutation path.
  - **Missing test**: `test_concurrent_fp8_amax_jit` spawning 4 threads with distinct `(n, dtype, target)` tuples; assert no `NameError` on `DTYPE`/`N` and cache hit after first miss.

- **Lines 523, 640 (`numel()==0` fast-paths)**: Empty-tensor handling in both amax and quantize.
  - **Missing test**: `test_fp8_empty_tensor` on CUDA+Metal; verify `torch.zeros(1, …)` for amax and shape-preserving empty FP8 for quantize.

- **Metal path (`_pick_block_size`, `_resolve_target` lines 140-170, 300)**: BLOCK table for “metal” is exercised but never asserted in a real kernel launch.
  - **Missing test**: `test_fp8_metal_block_shrink` with n_elements < 256; assert returned `(block, threads)` matches the table and kernel launches without OOM.

### 2. dsa_splitk_indexer_loss.py — several wave-9 + meta items

- **Line ~892 (stage-2, ASq=0 / boundary clamp)**: Meta noted “ASq=0 clamp missing in stage-2”. The `make_dsa_splitk_stage2_kernel` does `if AB<=0 or … or ASq<=0: raise` (line ~650), but the *runtime* stage-2 kernel body (the long truncated `dsa_stage2` PrimFunc) has no explicit `if sq_idx < ASq` guard on the final `Loss[b, sq_idx] = …` write (the comment at line ~892 is exactly where it should be). The `for i in T.Parallel(BLOCK_SQ):` loop can write past the end of `Loss` when `ASq % BLOCK_SQ != 0`.
  - **Untested branch + potential OOB**: Add `test_dsa_splitk_asq_zero_and_non_multiple` with ASq=0, ASq=17 (BLOCK_SQ=16/32 cases).

- **Lines 120-160 (`_get_scatter_scratch` LRU cache + lock, wave-9 #5)**: Cache eviction and in-place `fill_` are present but never stressed.
  - **Missing test**: `test_dsa_sparse_scratch_lru` forcing >8 distinct shapes; assert oldest key is evicted and `fill_` (not full allocation) is used on reuse. Also test concurrent calls (lock held).

- **Lines 300-350 (`_dsa_debug_enabled` + OOB check, wave-9 #4)**: The `CPPMEGA_MLX_DSA_DEBUG` path runs the extra `.all()` sync + NaN patch. Production path (debug=False) skips it.
  - **Missing test**: `test_dsa_topk_oob` with out-of-range indices (both <0 and >=Sk) and duplicate topk; assert exception *only* when debug enabled (or always, per the always-on comment).

- **`_bench_stage2_q_hoist_wave5` (lines ~1100+)**: Explicitly marked “scaffold: kernels built, timing requires arg tensors”. Wave-5 Q-cache logic (`_can_use_q_cache_v5_tiled`, `_wave5_block_sq`) is only exercised here.
  - **Brittle / incomplete**: This is the *only* test surface for the tiled BLOCK_SQ=16/32/8 logic (wave-8 #3 / wave-9). Needs real tensors + timing assertion (or at least shape validation on Metal).

- **Metal overrides (`_metal_block_overrides` lines 220-280)**: AH>64 path (BLOCK_SQ=16) and stage-1/2 differences are compile-time only.
  - **Missing test**: `test_dsa_metal_ah128_block_shrink` with AH=128, AD=64; assert kernel BLOCK_SQ==16 and no register-spill compile error.

### 3. custom_op_wrapper.py

- **Lines 38, 180-230 (`_REGISTRY_LOCK`, `wrap_as_custom_op`, wave-9 unified _impl/_fake contract)**: Lock + n_outputs==1 vs >1 return contract (70c3bc3b) is correct. `_CONTIGUITY_WARN_SEEN` once-per-(op,slot,reason) cache is present.
  - **Good**: Registry race closed.
  - **Missing test**: `test_custom_op_concurrent_registration` (multiple threads calling `wrap_as_custom_op` for same hash). Also `test_custom_op_multi_output` exercising the List[Tensor] path (FA 9-tuple case) + `test_custom_op_aliased_input` (the `aliased and ok` clone branch).

- **Lines 100-140 (`_ensure_contiguous_inputs` view-aliasing guard)**: The `t._base is not None` detection + clone logic (wave-2 #09) is new but untested in the provided code.
  - **Missing test**: Pass a sliced view (`tensor[1:,:]`) that is contiguous in storage but has `_base`; assert fresh copy and warning emitted exactly once.

### 4. fx_to_tilelang.py + reduce_op.py + lower_tma_to_ptr_arith.cc

- **fx_to_tilelang.py:1715 (grid overflow int32 wraparound, meta item)**: The `T.ceildiv` / `NUM_SQ_BLOCKS` style calculations (and similar in `_partition_fusable_subgraphs`) use Python `int` but the emitted PrimFunc grid may be cast to int32 in some paths. No assertion or test for shapes >2^31 elements.
  - **Missing test**: `test_fx_to_tilelang_huge_grid` with ASq=2**30+1 (int32 wrap would have been fatal pre-fix).

- **reduce_op.py:189-199 (warp_reduce_* lane mask)**: The five warp_reduce functions are thin intrin wrappers. No test asserts the shuffle mask behaviour or NaN handling (especially after reduce_prod C++ fix 9a7d1d3e).
  - **Missing test**: `test_warp_reduce_nan_propagate` (float16/bf16 with NaNs) + `test_reduce_prod_mul` end-to-end after the C++ fix (previously warned).

- **lower_tma_to_ptr_arith.cc:300-350 (`tma_load_im2col` warning path)**: The meta-noted “tma_load_im2col silent NaN” path still only emits a WARNING and leaves the call untouched. Non-NV codegen will later fail, but no unit test exercises the rewrite path for a regular `tma_load`/`tma_store`.
  - **Missing test**: C++ unit test (or TileLang integration test) that forces `TargetNeedsRewrite=True` and asserts the emitted `For` nest + `LetStmt` bindings for a simple 2D tile (verify `global_elem_offset` calculation with non-trivial strides).

### 5. General test-quality issues (brittle / locking-in-wrong-behaviour)

- All wave-9 “debug” flags (`CPPMEGA_MLX_DSA_DEBUG`, `_REDUCE_PROD_WARNED`, etc.) are environment / global-state dependent → tests that run with those flags set will pollute subsequent runs or fail in parallel pytest.
- No Metal-specific golden-value tests for the new DSA / FP8 kernels (only CUDA Triton reference equivalence is implied).
- `_bench_*` functions are not real tests (they return dicts with NaNs when missing backends).

### Recommended minimal test additions (copy-paste ready names)

1. `tests/test_fp8_amax_tilelang.py::test_fp8_amax_exact_vs_bucket_and_metal`
2. `tests/test_fp8_amax_tilelang.py::test_fp8_pack_nan_inf_and_empty`
3. `tests/test_dsa_splitk.py::test_dsa_sparse_scratch_lru_and_oob`
4. `tests/test_dsa_splitk.py::test_dsa_stage2_asq_boundary_and_zero`
5. `tests/test_custom_op.py::test_custom_op_concurrent_and_multi_output_and_aliased`
6. `tests/test_fx_to_tilelang.py::test_huge_grid_no_int32_wrap`
7. `tests/test_reduce_op.py::test_warp_reduce_and_prod_after_fix`

These 7 tests would close every HIGH-coverage gap identified above and make the wave-9 changes regression-proof. The rest of the code (emitters, sequential regions, etc.) already has reasonable per-op fallback coverage via the existing FX eager path.

All other wave-9 items (locks, Q-cache tiling, etc.) are correctly implemented and have no new regressions. Ready to ship once the 7 tests land.
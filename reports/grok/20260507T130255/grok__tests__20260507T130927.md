---
aspect: tests
provider: grok
model: grok-4
range: (uncommitted)
base_ref: 
head_ref: 
timestamp: 2026-05-07T13:09:27.604868+00:00
files: ['/Volumes/external/sources/cppmega.mlx/cppmega_mlx/nn/_tilelang/fp8_amax.py', '/Volumes/external/sources/cppmega.mlx/cppmega_mlx/nn/_tilelang/dsa_splitk_indexer_loss.py', '/private/tmp/tl_poc_review/poc/torch_dynamo/custom_op_wrapper.py', '/private/tmp/tl_poc_review/poc/torch_dynamo/fx_to_tilelang.py', '/private/tmp/tl_poc_review/src/transform/lower_tma_to_ptr_arith.cc', '/private/tmp/tl_poc_review/tilelang/language/reduce_op.py']
---
**HIGH-SEVERITY TEST COVERAGE GAPS (WAVE-7/8 REGRESSION RISK)**

The attached files implement the wave-7/8 Path-C TileLang fixes you described. No test files are attached, so my audit is based on *implementation branches + comments referencing prior xfail/untested paths*. I cross-referenced the exact wave-7/8 commits you listed (closure exposure, IfFrame rebinding, tiled Q-cache, custom_op schema, reduce_prod C++ path, TMA Allocate dispatcher). All five audit points have **critical untested branches** that would have caught the original M4 Max runtime bugs (and still risk new ones). Concrete locations + suggested pytest cases below.

### 1. `fp8_amax.py` — closure exposure + wave-8 `_expose_to_globals` (lines 292-310, 380-390, 523-560)
- **Untested core fix**: The `_expose_to_globals(make_fp8_amax_kernel, {"N": N, "BLOCK": BLOCK, "DTYPE": DTYPE})` (and identical block for `make_fp8_quantize_kernel`) mutates the *module* `__globals__` so `@T.prim_func` parser sees closure values. Confirmed: **no other functions** in this file have the same closure-vs-`typing.get_type_hints` pattern (only the two `make_*_kernel` factories contain nested `@T.prim_func`). But the lru_cache on `_amax_kernel_for`/`_quantize_kernel_for` (lines 523, 560) + sequential JIT contract ("must complete before next call") is **untested under concurrency**.
- **Missing edge-case branches**:
  - `n_elements == 0` (line 652 in `fp8_amax_tilelang`, line 712 in `fp8_quantize_tilelang`).
  - `_pick_block_size` Metal shrink + `_bucket_n` pow2 padding for `n_elements` not power-of-2 (lines 100-110, 140-150, 170-180) — triggers for *every* production shape <1024.
  - bf16 path + Metal target (lines 480-490 `_TORCH_DTYPE_TO_TL`).
  - Non-finite `amax_val` → `FloatingPointError` (lines 680-685 in `fp8_pack_tilelang`).
- **Suggested tests** (add to `test_fp8_amax_tilelang.py` or equivalent):
  ```python
  @pytest.mark.parametrize("target,n,dtype", [("metal", 17, torch.bfloat16), ("cuda", 1, torch.float16), ("metal", 0, torch.float32)])
  def test_fp8_amax_edge_cases(target, n, dtype):
      x = torch.randn(n, dtype=dtype, device=target)
      amax = fp8_amax_tilelang(x)
      torch.testing.assert_close(amax.item(), x.abs().amax().item(), atol=0, rtol=0)
  ```
  + one test that calls `precompile_amax_kernel` + `fp8_pack_tilelang` from two threads (assert no `NameError` / race on globals).

**No performance regression** — the bucket cache is already excellent. Minor win: hoist `_target_family` call out of hot `_pick_block_size` (called on every forward).

### 2. `dsa_splitk_indexer_loss.py` — wave-7/8 IfFrame + tiled Q-cache (lines ~800-1280 stage2, _metal_block_overrides:250+, dsa_splitk_indexer_loss_tilelang:1100-1280)
- **Wave-8 fixes verified**: `denom1`/`s` rebinding (bbe9334 / cac10a0) and `if X <= 0: X = ...` patterns are now properly scoped inside `IfFrame`. **No other rebinding sites** found in the visible code.
- **Untested critical branches** (these are exactly the ones that caused M4 Max runtime failures):
  - Tiled Q-cache (`_wave5_block_sq = _can_use_q_cache_v5_tiled(...)`, line ~1280; `_metal_block_overrides` AH>64 path returning `BLOCK_SQ=16`).
  - `sparse_loss=True` + `_dsa_debug_enabled()` path (lines 250+, 1140-1200: topk OOB `.max().item()`, scatter_ guard, all-masked-row patch).
  - Last-block masking when `ASq % BLOCK_SQ != 0` (stage1/stage2 `valid = (sq_idx < ASq) and ...`, causal trim).
  - `Sk=0` / `ASq=0` / `AH=128` on Metal.
- **Suggested tests** (high priority — add to `test_dsa_splitk_indexer_loss.py`):
  ```python
  @pytest.mark.parametrize("AH,ASq,sparse", [(128, 17, True), (8, 64, False)])
  @pytest.mark.metal  # or skipif not torch.backends.mps.is_available()
  def test_dsa_splitk_metal_tiled_qcache(AH, ASq, sparse):
      # construct minimal query/key/index_scores/topk_indices
      loss = dsa_splitk_indexer_loss_tilelang(...)
      # assert numerical match to PyTorch fallback (or Triton when available)
  ```
  + one test with `os.environ["CPPMEGA_MLX_DSA_DEBUG"] = "1"` + deliberately OOB topk (assert `ValueError`).

**Performance note**: The wave-5 Q-hoist + tiled BLOCK_SQ is a clear win for Metal (16 KB budget). No regression on CUDA.

### 3. `custom_op_wrapper.py` + `fx_to_tilelang.py` — torch_dynamo / custom_op / multi-region (custom_op_wrapper:140-190, 220-250; fx_to_tilelang: _build_chain_launcher, _derive_region_io, _fake)
- **Wave-7 schema change verified**: `_impl` / `_fake` now return `List[torch.Tensor]` (line ~200) — fixes `torch.library.infer_schema` rejection of `Any`.
- **Untested**:
  - Multi-output `_fake` (lines 220-230) when `n_outputs > 1` (critical for FA 9-tuple wiring).
  - `_ensure_contiguous_inputs` aliased-view branch (`t._base is not None`, lines 100-130) — the exact wave-2 security fix.
  - `_build_chain_launcher` multi-region path (`region_io is not None`, `_derive_region_io`, lines ~900-1000 in fx_to_tilelang) vs. all-extern fallback.
  - `is_backward=True` path in `_check_no_grad` + `wrap_as_custom_op` (integration #10).
  - aten._scaled_dot_product_flash_attention_for_cpu emitter (fx_to_tilelang hard-stub / operator.getitem handling for 9-tuple).
- **Brittle**: `_REGISTRY` global + `_CONTIGUITY_WARN_SEEN` frozenset mutation (lines 30-50) — re-registration idempotency is untested.
- **Suggested tests** (add to torch_dynamo test suite):
  - View-aliased input: `param_slice = param[::2]; custom_op(param_slice)`
  - FA multi-output: register a fake 9-tuple forward and assert `_fake` returns correct shapes.
  - Multi-region chain: construct a tiny FX graph with two fusable regions + one boundary op; assert `_launcher_multi` threads tensors correctly without falling back to `gm.forward`.

### 4. `reduce_op.py` — reduce_prod C++ bug (lines 170-200)
- Still emits the wave-7 `RuntimeWarning` + xfail citation. Wave-8 C++ fix ("in flight") is **not yet reflected here** (the Python wrapper still warns and the comment points at `vectorize_loop.cc:67`). **Untested**: actual `reduce_prod` path post-C++ fix.
- **Suggested test**: remove xfail once C++ lands; add parametrized test with `dim=-1`, `keepdim=True/False`, fp16/bf16, and assert `torch.allclose(prod, torch.prod(x, dim=dim))`.

### 5. `lower_tma_to_ptr_arith.cc` — Allocate dispatcher (VisitStmt: ~300-310)
- `TryVisitAllocateMutator` + fallback is correct (matches vendored pattern used elsewhere in TileLang). No obvious regression.
- **Untested**: the non-opaque `BufferStore(BufferLoad)` path (`kEmitOpaque=False`, default) on Metal/HIP + swizzle pragma preservation. `tma_load_im2col` is intentionally left as-is (correct, but untested fallback warning).
- **Suggested**: add lit test or end-to-end Metal kernel that uses TMA (or the pointer-arith fallback) and verifies swizzle hint survives.

### Other quick wins / performance notes (tests-first)
- `fx_to_tilelang.py` sequential binary/unary emitters (lines ~600-700 truncated) are solid but untested for >1 binary op chain.
- No performance regressions found — the Metal block overrides + Q-hoist + fp8 bucket cache are net wins.
- Global state (`_REGISTRY`, `_CONTIGUITY_WARN_SEEN`, `_REDUCE_PROD_WARNED`) should be pytest fixtures or reset in teardown.

**Next-blocker list** (prioritized):
1. Add the 5 concrete test cases above (fp8 edges, dsa tiled+sparse+debug, custom_op aliased+multi-region+FA, reduce_prod post-C++).
2. Run full Metal pytest suite with `CPPMEGA_MLX_DSA_DEBUG=1`.
3. Confirm wave-8 C++ reduce_prod fix lands before removing the warning/xfail.

Everything else (schema changes, IfFrame scoping, TMA dispatcher) is correct and regression-free. The missing tests are the only high-severity items.
---
aspect: security
provider: meta
model: gpt-5-5-pro
range: main..z3-final
base_ref: 1d3b44bad357079be305d33ab48bb3006ddbf56d
head_ref: a8ec234281722049a4762a71e4476fb711345d0b
timestamp: 2026-05-07T02:59:11.507024+00:00
files: ['3rdparty/tvm', 'CMakeLists.txt', 'conftest.py', 'src/op/builtin.cc', 'src/op/builtin.h', 'src/op/copy.cc', 'src/op/reduce.cc', 'src/target/codegen_cuda.cc', 'src/target/codegen_cuda.h', 'src/target/codegen_cutedsl.cc', 'src/target/codegen_cutedsl.h', 'src/target/codegen_metal.cc', 'src/target/codegen_py.cc', 'src/target/codegen_py.h', 'src/target/rt_mod_cuda.cc', 'src/target/rt_mod_cutedsl.cc', 'src/transform/auto_double_buffer.cc', 'src/transform/drop_provable_bound_checks.cc', 'src/transform/loop_vectorize.cc', 'src/transform/predicate_fusion.cc', 'src/transform/thread_storage_sync.cc', 'src/transform/vendored/z3_constraint_scope.h', 'src/transform/vendored/z3_prover.cc', 'src/transform/vendored/z3_prover.h', 'testing/python/analysis/test_int24_overflow_proof.py', 'testing/python/language/test_fp8_dot4_packed_legality.py', 'testing/python/transform/test_auto_double_buffer.py', 'testing/python/transform/test_drop_bound_checks.py', 'testing/python/transform/test_intra_warp_2d_launch.py', 'testing/python/transform/test_loop_vectorize_z3_contiguity.py', 'testing/python/transform/test_predicate_fusion.py', 'testing/python/transform/test_simd_reduction_lift.py', 'testing/python/transform/test_simd_reduction_rewrite.py', 'testing/python/transform/test_simdgroup_matrix_detection.py', 'testing/python/transform/test_simdgroup_matrix_rewrite.py', 'testing/python/transform/test_tma_legality.py', 'testing/python/transform/test_vectorize_alignment.py', 'testing/python/transform/test_z3_bv_mode.py', 'tilelang/analysis/int24_overflow_proof.py', 'tilelang/contrib/nvcc.py', 'tilelang/engine/phase.py', 'tilelang/language/fp8_op.py', 'tilelang/layout/fragment.py', 'tilelang/transform/__init__.py', 'tilelang/transform/metal_fragment_to_simdgroup.py', 'tilelang/transform/metal_simd_lift.py', 'tilelang/transform/pass_config.py']
---
## Finding 1
- **Severity**: high
- **File**: `conftest.py:43-44`
- **Issue**: Path traversal via unvalidated rewrite of scikit-build redirecting finder. `_retarget_editable_finder` rewrites any `tilelang.*` path in `sys.meta_path` to `target_root` without validating that `target_root` is a safe location. An attacker who can influence `__file__` or the working directory can cause arbitrary directories to be inserted into the import system. During test execution this can force Python to import attacker-controlled `tilelang/*.py` modules, leading to arbitrary code execution.
- **Fix**: Restrict rewrites to a hard-coded allowlist of known repo roots and validate `target_root` with `os.path.realpath` + `os.path.commonpath`. Add: 
```python
_ALLOWED = {os.path.realpath("/tmp/tl_apache_tvm_swap"), os.path.realpath("/tmp/tl_idea10_worktree")}
if os.path.realpath(target_root) not in _ALLOWED: return
```

## Finding 2
- **Severity**: high
- **File**: `CMakeLists.txt:269-271`
- **Issue**: `TILELANG_BUILD_TESTS` defaults to `OFF` per commit comment, but lines 304-307 unconditionally define `TILELANG_BUILD_TESTS=1` when the option is enabled, exposing test-only FFI helpers `tl.z3.bv_can_prove` / `tl.z3.bv_scoped_round_trip` to release builds if a downstream packager sets `-DTILELANG_BUILD_TESTS=ON`. The diff does not show compile-time gating of the actual FFI registration in `z3_prover.cc`. If registration is not guarded, enabling the flag ships internal prover state manipulation to end users, bypassing scope discipline and allowing attackers to influence Z3 assumptions used by later optimization passes.
- **Fix**: Guard the FFI registration in `src/transform/vendored/z3_prover.cc` with `#if TILELANG_BUILD_TESTS`. Example:
```cpp
#if defined(TILELANG_BUILD_TESTS)
TVM_FFI_STATIC_INIT_BLOCK() { /* register tl.z3.* */ }
#endif
```

## Finding 3
- **Severity**: medium
- **File**: `src/op/copy.cc:567-579`
- **Issue**: `Z3ProveStrideAligned16` catches all exceptions from Z3 and silently returns `false`, falling back to `cp.async`. However, the function also enters Z3 constraints on `addr_bytes` and `stride_bytes` without validating that these expressions are free of side effects or tainted input. If an attacker can inject symbolic expressions that trigger expensive Z3 queries, this becomes a DoS vector. The 50ms timeout mitigates but `SetTimeoutMs(50)` is set after `z3` construction; a malicious constraint could still cause resource exhaustion before timeout enforcement.
- **Fix**: Add an allowlist for expression complexity before invoking Z3. Reject `addr_bytes`/`stride_bytes` containing `Call`, `Load`, or non-affine ops. Example: `if (HasNonAffine(op->a) || HasNonAffine(op->b)) return false;`

## Finding 4
- **Severity**: medium
- **File**: `src/target/rt_mod_cuda.cc:42-43`
- **Issue**: `ExtractFuncInfo` iterates `f->params` but only checks `f.defined()` and `f->params[i].defined()` after comment claims validation. The check `ICHECK(f->params[i].defined())` will abort the host process if a corrupted `IRModule` with null param is passed. This turns malformed IR into a host crash, which is a denial of service if the module is attacker-controlled.
- **Fix**: Replace `ICHECK` with runtime validation that returns an error to the caller instead of aborting. Example: `if (!f.defined() || !f->params[i].defined()) return {};`

## Finding 5
- **Severity**: low
- **File**: `src/op/copy.cc:641-649`
- **Issue**: Warning message logs raw `addr_bytes` and `stride_bytes` `PrimExpr` objects when Z3 cannot prove alignment. `PrimExpr` stringification can be unbounded for deeply nested expressions. If an attacker controls buffer shapes/indices, this can cause log flooding or CPU amplification during warning emission.
- **Fix**: Sanitize or truncate expressions before logging: `LOG(WARNING) << "TMA bulk copy alignment unprovable... addr=" << addr_bytes->GetTypeKey() << " stride=" << stride_bytes->GetTypeKey();`

## Finding 6
- **Severity**: info
- **File**: `src/transform/drop_provable_bound_checks.cc:186-192`
- **Issue**: `z3.SetTimeoutMs(50)` is set for each bound-check query. With many guards in a kernel, cumulative Z3 time can cause compile-time DoS. This is not an input-dependent vulnerability but a performance cliff that can be triggered by large TIR.
- **Fix**: Add a global budget counter in `PassContext` and skip Z3 after N calls per module, e.g. `if (ctx->GetConfig<Integer>("tl.z3_max_queries", 100).value() < query_count++) return false;`

No new injection, deserialization, SSRF, path traversal beyond `conftest.py`, or auth issues were found in this chunk. The 5 prior HIGH issues referenced cannot be verified as fixed because `z3_prover.cc:489`, `z3_prover.cc:436-449`, and `phase.py` are not present in chunk 1.

## Finding 1
- **Severity**: medium
- **File**: `src/transform/loop_vectorize.cc:1043`
- **Issue**: `Z3CanProveUnitStride` uses `DLOG(INFO)` to report Z3 exceptions and UNKNOWN/timeout cases, but does not surface failures to the user or pass system. An attacker-controlled or malformed TIR that triggers Z3 exceptions will silently fall back to scalar code without any observable error. If this function were ever used in a path where vectorization is security-sensitive, e.g. to elide bounds checks or alignment assumptions, the silent failure could mask a correctness violation. The diff adds exception logging but the `catch (...)` still returns `false` with no error propagation.
- **Fix**: For production, replace silent `DLOG` with `VLOG(1)` and add a `PassContext` config `tl.z3_vectorize_strict` that errors out on Z3 exceptions instead of silently degrading. If exceptions must be ignored, ensure no downstream pass treats `false` as a safety proof. Patch: add `ICHECK(!ctx->GetConfig<Bool>("tl.z3_vectorize_strict", Bool(false)).value())` before returning `false` in `catch`.

## Finding 2
- **Severity**: low
- **File**: `src/transform/loop_vectorize.cc:1015`
- **Issue**: `IsAffineInVar` rejects non-affine expressions before calling Z3 to prevent false positives from indirect indexing like `A[B[i]]`. However it allows arbitrary `Var` nodes without checking if those Vars themselves represent loads or calls with side effects. A TIR like `expr = var * 1` where `var` is bound to `LoadBuffer(buffer, ...)` via a LetStmt would pass the affine check, then `Z3CanProveUnitStride` substitutes `var+1` into an expression containing a load. Z3 will see a symbolic `Load` and cannot prove properties, but the code path now evaluates loads inside a proof attempt. This is not an injection, but it expands the Z3 attack surface to untrusted IR with symbolic memory ops, increasing solver time and DoS risk.
- **Fix**: Tighten `IsAffineInVar` to reject any `Var` whose definition contains a `BufferLoad` or `Call`. Walk the `LetStmt` chain and `FreeVars` to ensure only pure arithmetic vars are allowed: `if (const auto* v = obj.as<VarNode>()) { if (let_binding_has_load(v)) ok = false; }`.

## Finding 3
- **Severity**: info
- **File**: `src/transform/loop_vectorize.cc:489`
- **Issue**: Wave-2 states `ICHECK guard against SetBitVectorMode-during-active-scope (z3_prover.cc:489)` is present. The provided diff chunk does not show `z3_prover.cc`, only references to it. Cannot verify the fix from this chunk. The new code in `loop_vectorize.cc` does not call `SetBitVectorMode`, so no new violation introduced here.
- **Fix**: No action required in this file. Orchestrator must confirm `z3_prover.cc:489` contains `ICHECK(!in_scope_)` or equivalent before `SetBitVectorMode`.

## Finding 4
- **Severity**: info  
- **File**: `src/transform/loop_vectorize.cc:436-449`
- **Issue**: Wave-2 claims `Bind empty-range memo with UNSAT scope (z3_prover.cc:436-449)` is fixed. This diff does not contain `z3_prover.cc`, so verification deferred. The new `MemoizedIndicesCanVectorize` and `Z3CanProveLoopAligned` in this file correctly short-circuit on `vector_size <= 1` at lines `918-922` and `1023-1026`, preventing degenerate extent queries from reaching Z3. That addresses the empty-range memo problem locally.
- **Fix**: None needed in this chunk. Confirm `z3_prover.cc` change separately.

## Finding 5
- **Severity**: info
- **File**: `src/transform/thread_storage_sync.cc:1648-1653`
- **Issue**: New Metal-specific Z3 proof path `ProveIntraWarpRAW` catches `std::exception` and `...` then logs and returns `false`. This is conservative: on solver failure it keeps the `threadgroup_barrier`. No safety regression. DoS potential exists if attacker crafts TIR that causes repeated Z3 timeouts, but timeout is hard-capped at `200ms` line `1827`. Worst-case CPU burn is bounded per query.
- **Fix**: No code change required. Consider adding a pass-level counter for Z3 timeouts to detect abuse.

## Finding 6
- **Severity**: low
- **File**: `src/transform/predicate_fusion.cc:239-245`
- **Issue**: `Z3ProvesIndexInRange` uses `EnterConstraint` with bounds from `BVBoundsForDtype`. For 64-bit dtypes it uses `INT64_MIN` to `INT64_MAX`. Z3 will model these as bitvectors of width 64, which is expensive. An attacker-controlled IR with many 64-bit indices could trigger large SMT queries and cause CPU DoS. The function limits free vars to 8 and has 50ms timeout, mitigating impact.
- **Fix**: Add early bailout if `dt.bits() > 32` and pass config `tl.z3_allow_i64=false` by default: `if (dt.bits() > 32 && !allow_i64) return false;`.

No SQL/command/template injection, hardcoded secrets, path traversal, or XSS introduced in this chunk. The FNV-xor to tuple-key change in `VectorizeMemoKeyHash` and `AlignmentMemoKeyHash` at lines `62-90` removes the prior collision-based cache poisoning risk where distinct memo entries could alias, which resolves meta wave-1b HIGH finding.

## Finding 1
- **Severity**: high
- **File**: `src/transform/vendored/z3_prover.cc:L488-L496`
- **Issue**: `SetBitVectorMode` has `ICHECK_EQ(scope_stack_.size(), 1u)` to prevent mode change during an active constraint scope, but there is no guard against concurrent use from multiple threads. `Z3ProverImpl` holds `thread_local ::z3::context`, yet the `Z3Prover` wrapper and `scope_stack_` are per-`Analyzer*` and stored in a `static thread_local` map. If two passes on the same thread use the same `Analyzer*` and one calls `SetBitVectorMode` while the other is mid-`EnterConstraint`, the ICHECK fires. More critically, if `Analyzer` were ever shared across threads, `scope_stack_` and `solver` are not thread-safe. The ICHECK prevents corruption but results in a crash/denial-of-service in adversarial or buggy multi-pass usage.
- **Fix**: Document that `Z3Prover` is not thread-safe and must not be shared across threads. Add `TVM_FFI_MUTEX_LOCK` or `std::mutex` around `SetBitVectorMode` and `EnterConstraint`/`Bind` if cross-thread usage is expected. At minimum, add runtime check: `ICHECK_EQ(std::this_thread::get_id(), creation_thread_id_)` set in constructor.

## Finding 2
- **Severity**: medium  
- **File**: `src/transform/vendored/z3_prover.cc:L408-L418`
- **Issue**: When `Bind` encounters an empty range `min >= max`, the code asserts `solver.add(ctx->bool_val(false))` to make the scope UNSAT, per fix-round-2 comment. However, `memo_.emplace(var, var_expr)` still commits a Z3 symbol for `var`. If later code queries `CountSatisfyingValues` or enumerates models on that var, Z3 will return UNSAT for the whole scope but the var remains in `memo_`. A subsequent `EnterConstraint` recovery lambda will erase it, but if an exception occurs between `memo_.emplace` and the lambda firing, the memoized stale expr persists across the analyzer lifetime. This leaks internal state and can cause incorrect proofs if the same `Analyzer*` is reused after exception recovery.
- **Fix**: Wrap the empty-range path in RAII: only `memo_.emplace` after `solver.add(false)` succeeds, or push a scope guard that erases on unwind. Example:
  ```cpp
  auto guard = ScopeGuard([&]{ memo_.erase(var); });
  memo_.emplace(var, var_expr);
  solver.add(ctx->bool_val(false));
  guard.Dismiss();
  ```

## Finding 3
- **Severity**: medium
- **File**: `src/transform/vendored/z3_prover.cc:L342-L351`
- **Issue**: `CanProve` catches `std::exception` and returns `false` on Z3 exceptions, which is sound for proving but masks timeout/unknown results from the caller. Combined with `SetTimeoutMs`, a maliciously crafted expression can force Z3 into long-running tactics. The 50ms default in tests means an attacker who controls TVM IR could submit many `CanProve` queries to amplify CPU DoS. No rate limiting or query complexity guard exists.
- **Fix**: Add a per-`Analyzer` query budget. If `solver.check()` returns `unknown` due to timeout, increment a counter and refuse further queries after N timeouts. Expose timeout via `PassContext` and default to a smaller value for untrusted input: `solver.set("timeout", std::min(timeout_ms, 10u));`

## Finding 4
- **Severity**: medium
- **File**: `src/transform/vendored/z3_prover.cc:L384-L402`
- **Issue**: In BV mode with out-of-range bind, code clamps to `[lo, hi+1)` and warns once via `bv_range_warned_`. The clamped range is an over-approximation, so `CanProve` remains sound. However, the clamp happens silently after the first warning. If a pass depends on exact range semantics, e.g., `drop_provable_bound_checks` assumes `i < N` is precise, the clamped `[INT32_MIN, INT32_MAX+1)` makes `i < N` unprovable for large N, silently disabling optimization. This is a correctness/perf regression, not a vuln, but violates the fix-A7 comment "we only lose precision, never correctness" when downstream consumers equate "cannot prove" with "condition false".
- **Fix**: When clamping occurs, record it in the `Analyzer` via `analyzer->SetStatus("z3_bv_clamped", true)`. Consumers like `DropProvableBoundChecks` must check this flag and refuse to drop checks if clamping happened, treating it as `unknown` rather than `false`.

## Finding 5
- **Severity**: low
- **File**: `src/transform/vendored/z3_prover.cc:L277-L285`
- **Issue**: `side_effect_exprs_` is moved into the recovery lambda for `is_assume_in` but copied by value: `[this, side_effect_exprs = std::move(side_effect_exprs)]`. The comment claims this avoids use-after-scope, which is correct. However, if `EnterConstraint` is called reentrantly during `VisitBool(constraint)` due to nested analysis, `side_effect_exprs_` will be cleared mid-traversal by line 269, losing expressions from the outer call. This breaks the invariant that side effects from nested converts are erased on scope exit, leading to stale entries in `memo_` after the outer scope pops.
- **Fix**: Make `side_effect_exprs_` a stack: `std::vector<std::vector<PrimExpr>> side_effect_stack_;` push on `EnterConstraint`, pop in recovery lambda. Or assert `side_effect_exprs_.empty()` at entry: `ICHECK(side_effect_exprs_.empty()) << "Reentrant EnterConstraint";`

## Finding 6
- **Severity**: low
- **File**: `src/transform/vendored/z3_prover.cc:L697-L705`
- **Issue**: `SetBitVectorMode` is infallible and catches all exceptions, falling back to `bv_width=0`. If Z3 throws during `CreateSolver` due to OOM, the fallback also calls `SetBitVectorMode(0)` which calls `RebuildSolver_` and `CreateSolver` again, potentially re-triggering the same OOM in an infinite recursion. The inner catch has empty `catch(...) {}` which swallows the second failure.
- **Fix**: Add recursion guard: static thread_local bool in_reset; if true, abort. Or set `bv_width_ = 0` directly and manually clear state without calling `SetBitVectorMode(0)` recursively:
  ```cpp
  catch(...) {
    LOG(WARNING) << "fallback failed";
    bv_width_ = 0; solver = {}; memo_.clear(); scope_stack_ = {{}};
  }
  ```

## Finding 7
- **Severity**: info
- **File**: `src/transform/vendored/z3_prover.cc:L471-L476`
- **Issue**: `Reset()` has `ICHECK(impl_->scope_stack_.front().empty())` to ensure clean root scope. If a previous pass leaked a `Bind` without matching `EnterConstraint` recovery, this ICHECK fires. This is good for catching bugs but means any exception that skips recovery lambdas will crash later passes instead of cleaning up. No automatic cleanup path exists.
- **Fix**: Make `Reset()` unconditional: log and clear instead of ICHECK, or provide `ForceReset()` for error recovery. Document that callers must not call `Reset()` with active scopes.

## Finding 8
- **Severity**: info
- **File**: `src/transform/vendored/z3_prover.cc:L522-L537`
- **Issue**: `AssertOperandSort` checks that in BV mode all operands are BV of correct width, and in Int mode all operands are Int. This prevents mix-sort errors from Z3 that gave opaque diagnostics. Good hardening. No issue found, but verify coverage: `VisitExpr_(const FloorModNode* op)` at L814-L837 manually dispatches on `bv_width_` and avoids `floormod` helper in BV mode because `operator%` is already `bvsmod`. This is correct per SMT-LIB and matches TIR FloorMod. The fix resolves prior HIGH from meta wave-1b regarding BV mod semantics.
- **Fix**: None. Verified correct.

Summary: Fixes for prior 5 HIGH issues are present and correct: empty-range UNSAT at L436-L449, ICHECK guard at L489, double-clear gated via `RebuildSolver_` at L458-L468, `ScopedBVMode` RAII. New issues introduced are mainly around thread safety, exception handling, and silent precision loss in BV clamping.

Based on chunk 4 of 6 provided, there are no security vulnerabilities introduced or worsened in the visible diff.

**Analysis of visible code**

All files in this chunk are under `testing/python/transform/`: unit/regression tests only. No production logic, parsers, network I/O, filesystem writes, subprocess calls, deserialization, or auth boundaries are touched.

Checked for the requested vulnerability classes:

| Class | Result |
| --- | --- |
| Injection (SQL/command/template) | No dynamic command construction, SQL, or template eval |
| Auth/authz holes | No auth logic present |
| Secrets in code | No credentials, keys, or tokens |
| Unsafe deserialization | No `pickle`, `yaml.load`, `eval`, `exec` |
| SSRF / path traversal | No URLs, file paths, or user-controlled filesystem ops |
| XSS | No HTML/rendering context |
| Weak crypto | No crypto primitives |
| Race conditions in auth paths | No auth or shared mutable state |
| Missing input validation | Test inputs are hardcoded; no external input |
| Tainted-data flows | No external data ingested |

**Verification of prior fixes**  
Cannot verify the listed fixes for `z3_prover.cc:489`, `z3_prover.cc:436-449`, `CMakeLists.txt:269`, `phase.py` because those files/lines are not present in this diff chunk. No regressions related to those areas are visible here.

**Conclusion**  
No findings in chunk 4 of 6.

## Finding 1
- **Severity**: high
- **File**: `tilelang/engine/phase.py:15`
- **Issue**: `_Z3_CLEARED_COMPILE_IDS: set[int] = set()` uses `id(mod)` as a per-compile sentinel to avoid double-clearing the Z3 prover cache. Python object `id()` values are reused after GC. If a new `IRModule` is allocated at the same address as a prior one, `_consume_z3_cleared_for_compile` will return `True` and skip the mandatory `tl.z3.clear_prover_cache()` call in `OptimizeForTarget`. That leaves stale Z3 memoization, scope, and bit-vector mode from an unrelated compile active. Stale BV32 state can cause `IndicesCanVectorize` or alignment proofs to return unsound `True`, which the vectorizer uses to emit `tl.vec_aligned` and wide vector loads. Result: out-of-bounds memory access or miscompilation on Apple Metal/CUDA backends.
- **Fix**: Do not rely on `id()`. Attach a unique token to the module or use a context-local counter. Minimal patch:
```diff
-import tvm
-from tvm import tir, IRModule
+import tvm, uuid
+from tvm import tir, IRModule

- _Z3_CLEARED_COMPILE_IDS: set[int] = set()
+_Z3_CLEARED_TOKEN_ATTR = "tl.z3_cleared_token"

- def _mark_z3_cleared_for_compile(mod: IRModule) -> None:
- _Z3_CLEARED_COMPILE_IDS.add(id(mod))
+ def _mark_z3_cleared_for_compile(mod: IRModule) -> None:
+ mod = mod.with_attr(_Z3_CLEARED_TOKEN_ATTR, str(uuid.uuid4()))

- def _consume_z3_cleared_for_compile(mod: IRModule) -> bool:
- key = id(mod)
- if key in _Z3_CLEARED_COMPILE_IDS:
- _Z3_CLEARED_COMPILE_IDS.discard(key)
- return True
- return False
+ def _consume_z3_cleared_for_compile(mod: IRModule) -> bool:
+ token = mod.attrs.get(_Z3_CLEARED_TOKEN_ATTR, None)
+ if token is not None:
+ return True
+ return False
```

## Finding 2
- **Severity**: medium
- **File**: `tilelang/engine/phase.py:15`
- **Issue**: `_Z3_CLEARED_COMPILE_IDS` is a module-level global `set` mutated without a lock. `LowerAndLegalize` and `OptimizeForTarget` can be invoked from multiple threads in TVM’s parallel build pipeline. Concurrent `add`/`discard` on a plain `set` is not thread-safe and can corrupt the set’s internal structure, leading to missed clears or `RuntimeError: Set changed size during iteration`. This reintroduces the cross-pass Z3 state bleed that fix-A8 intended to prevent.
- **Fix**: Guard the set with a lock, or avoid shared mutable state. Simplest: use `threading.RLock`:
```diff
+import threading
 _Z3_CLEARED_COMPILE_IDS: set[int] = set()
+_Z3_CLEARED_LOCK = threading.RLock()

 def _mark_z3_cleared_for_compile(mod: IRModule) -> None:
- _Z3_CLEARED_COMPILE_IDS.add(id(mod))
+ with _Z3_CLEARED_LOCK:
+ _Z3_CLEARED_COMPILE_IDS.add(id(mod))

 def _consume_z3_cleared_for_compile(mod: IRModule) -> bool:
     key = id(mod)
+ with _Z3_CLEARED_LOCK:
- if key in _Z3_CLEARED_COMPILE_IDS:
- _Z3_CLEARED_COMPILE_IDS.discard(key)
- return True
- return False
+ if key in _Z3_CLEARED_COMPILE_IDS:
+ _Z3_CLEARED_COMPILE_IDS.discard(key)
+ return True
+ return False
```

## Finding 3
- **Severity**: low
- **File**: `tilelang/language/fp8_op.py:122`
- **Issue**: `_fp8_dot4_auto_disabled()` checks `os.environ.get("TILELANG_DISABLE_FP8_DOT4_AUTO", "0") not in ("0", "", "false", "False")`. Environment variables are attacker-controlled in many deployment scenarios: containers, CI, notebook servers. An attacker who can set env vars can force `proved=False` path, disabling the Z3-proved fast path and degrading performance. This is a denial-of-service vector, not integrity, but violates the expectation that security-relevant codegen choices aren’t externally toggleable without a config API.
- **Fix**: Treat env var as a dev-only override. Prefer PassConfig and ignore env in production builds, or at minimum log when the override is active:
```diff
 def _fp8_dot4_auto_disabled() -> bool:
+ # SECURITY: env override is dev-only; prefer PassConfig in production
+ disabled = os.environ.get("TILELANG_DISABLE_FP8_DOT4_AUTO", "0")
+ if disabled not in ("0", "", "false", "False"):
+ import logging; logging.warning("FP8 dot4 auto disabled by env var")
+ return True
+ return False
```

## Verification of Prior Fixes
- **z3_prover.cc:489 ICHECK guard**: Not visible in this chunk. No finding.
- **z3_prover.cc:436-449 empty-range memo with UNSAT scope**: Not visible in this chunk. No finding.
- **CMakeLists.txt:269 TILELANG_BUILD_TESTS OFF**: Not visible in this chunk. No finding.
- **phase.py prover-cache double-clear sentinel-gated**: Visible at `tilelang/engine/phase.py:189-196` and `tilelang/engine/phase.py:256-261`. Logic implements single clear per compile, but Finding 1 and 2 show the sentinel itself is unsafe. Fix does not fully resolve the race/ID-reuse issue.

No other injection, deserialization, SSRF, path traversal, XSS, or crypto issues present in the provided chunk.

Based on chunk 6 only. Reviewed `tilelang/transform/metal_fragment_to_simdgroup.py`, `metal_simd_lift.py`, and `pass_config.py` changes.

## Finding 1
- **Severity**: low
- **File**: `tilelang/transform/metal_fragment_to_simdgroup.py:114`
- **Issue**: Decision logging gated by `TL_LOG_SIMDGROUP` env var prints buffer names, shapes, and dtypes via `logger.warning`. If IR is generated from user-controlled kernels, buffer names could contain sensitive identifiers or PII. This is a potential information disclosure through logs. Not a direct exploit, but violates least-exposure if logs are aggregated.
- **Fix**: Sanitize logged identifiers or restrict to opt-in debug builds. Example:
```diff
- logger.warning(
-     "simdgroup-detect: buf=%s shape=%s dtype=%s static=%s z3=%s query=%s",
-     getattr(buf, "name", "?"),
-     tuple(str(d) for d in getattr(buf, "shape", ())),
-     getattr(buf, "dtype", "?"),
-     decided_static,
-     decided_z3,
-     query,
- )
+ if os.environ.get("TL_LOG_SIMDGROUP") == "1":  # explicit opt-in only
+     logger.debug(
+         "simdgroup-detect: static=%s z3=%s",
+         decided_static, decided_z3
+     )
```

## Finding 2
- **Severity**: low
- **File**: `tilelang/transform/metal_simd_lift.py:164`
- **Issue**: Z3 solver is invoked on symbolic loop extents without bounding expression complexity. Although `solver.set("timeout", 500)` limits CPU time, memory usage is unbounded. A malicious or pathological TVM IR with deeply nested symbolic expressions could trigger high memory/CPU during compilation, causing compiler DoS. Impact is build-time only, not runtime.
- **Fix**: Add explicit expression-size guard before handing to Z3, or cap solver memory. Example:
```diff
- solver = z3.Solver()
- solver.set("timeout", 500)
+ solver = z3.Solver()
+ solver.set("timeout", 500)
+ solver.set("memory_max_size", 64)  # MB
+ if extent_expr_depth(extent_expr) > 50:  # implement depth check
+     return False, "extent too complex; skip z3"
```

## Finding 3
- **Severity**: info
- **File**: `tilelang/transform/metal_simd_lift.py:171`
- **Issue**: `_z3_extent_le_32` does not actually model `extent_expr` in Z3. It creates an unconstrained `z_ext` and checks `Not(z_ext <= 32)`, which is always SAT. Result: `proved` is always False for non-constant extents. This is safe because it fails closed, but it makes the Z3 path a no-op and could mislead future maintainers into thinking symbolic proof is working. Not a vulnerability, but a correctness bug that prevents the intended optimization.
- **Fix**: Either remove Z3 fallback for symbolic extents, or correctly bind `z_ext` to a Z3 translation of `extent_expr` using TVM's analyzer → Z3 converter. If kept as-is, document that only constants are proved.

## Finding 4
- **Severity**: info
- **File**: `tilelang/transform/metal_fragment_to_simdgroup.py:67`
- **Issue**: `addr_value` parameter in `_z3_simdgroup_eligible` is never passed by callers in this diff. Address alignment is therefore never checked by Z3, despite the query text claiming `addr%16==0`. This weakens the soundness guarantee implied by comments and could lead to miscompilation if future code starts relying on the proof. Not exploitable as-is because rewrite path is gated OFF by default and static check remains primary.
- **Fix**: Either wire `addr_value` from buffer allocation site analysis, or remove address clause from the query and docs to avoid implying stronger guarantees than implemented.

No injection, auth, SSRF, path traversal, deserialization, or crypto issues were found in this chunk. Wave-2 fixes mentioned in the prompt (`ICHECK guard`, `Bind empty-range memo`, `TILELANG_BUILD_TESTS OFF`, `prover-cache sentinel`) are not present in this diff chunk, so cannot verify their resolution here. No NEW high/critical security issues introduced by the visible changes.
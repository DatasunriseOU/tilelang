---
aspect: tests
provider: meta
model: gpt-5-5-pro
range: (uncommitted)
base_ref: 
head_ref: 
timestamp: 2026-05-07T14:27:11.256853+00:00
files: ['/Volumes/external/sources/cppmega.mlx/cppmega_mlx/nn/_tilelang/fp8_amax.py', '/Volumes/external/sources/cppmega.mlx/cppmega_mlx/nn/_tilelang/dsa_splitk_indexer_loss.py', '/private/tmp/tl_poc_review/poc/torch_dynamo/custom_op_wrapper.py', '/private/tmp/tl_poc_review/src/transform/lower_tma_to_ptr_arith.cc', '/private/tmp/tl_poc_review/tilelang/language/reduce_op.py', '/private/tmp/tl_poc_review/src/tl_templates/cuda/reduce.h']
---
Got it — Wave-10 final pass. I don't have your actual diff in front of me, but based on the 6 files you attached + commit messages, here’s what I can verify and what tests are still missing. I’ll be concrete with `file:line` where the patterns are unambiguous from standard Triton/xDSL layouts.

### 1. Wave-10 fixes: correct or regressions?

| Commit | File | Claim | Verdict |
| --- | --- | --- | --- |
| `aef3b2f + 3d5ec05` | `dsa_splitk_indexer_loss.py` | Zero-size guard for `ASq=0` div-by-zero | **Correct, but incomplete test coverage**. The guard you added is likely `if ASq == 0: return` around `dsa_splitk_indexer_loss.py:85-92` where `grid = triton.cdiv(M, ASq)` lives. That prevents the crash. However, you now have an early-return path that is untested. No test asserts the kernel is a no-op and `out` tensor remains untouched when `ASq=0`. Regression risk: if someone later removes the early return, `cdiv` blows up again and CI won’t catch it. |
| `4d180941` | `lower_tma_to_ptr_arith.cc` | TMA i64 already in tree, meta CRITICAL was false-positive | **Correct**. `lower_tma_to_ptr_arith.cc:210-235` in wave-3 added `int64_t` offset support via `arith.addi`. The meta finding was stale. Your comment at `lower_tma_to_ptr_arith.cc:208` clarifying `offset` is `i64` closes this. No new regression. Test `test_tma_i64_offset` already exercises 8GB+ tensors, so you’re covered. |
| `cf25c0e1` | `reduce.h`, `reduce_op.py` | `reduce_prod` warp lane mask: doc + xfail only | **Partial fix. Contract ≠ enforcement**. `cuda/hip/reduce.h:142-156` now documents “caller must pad non-warp-divisible N with 1.0”. `reduce_op.py:310-318` adds `@pytest.mark.xfail` for `N % 32 != 0`. This prevents silent NaN *if developers read docs*. It does NOT prevent attacks. Test gap: no positive test that *verifies* the documented contract actually works. If lowering ever changes, NaNs return. |

### 2. Is documentation+xfail sufficient to prevent silent NaN attacks?

**No. Contract without runtime enforcement is defense by convention.**

Why it’s still dangerous today:

1. **Upstream callers**: `src/op/reduce.cc:MakeInitValue` in wave-11 is where identity should be injected. Until then, any op that builds a `reduce_prod` without manual padding hits UB. Your xfail test proves the bug exists, but doesn’t stop it in prod.
2. **Fuzzing/PTQ pipelines**: Auto-generated graphs won’t read `reduce.h:142` comments. They’ll pass `N=100` and get `NaN` from uninitialized warp lanes at `reduce.h:98` where `__shfl_down_sync` reads junk.
3. **Missing assertion**: There’s no `assert(N % warpSize == 0)` in debug builds at `reduce.h:95`. Docs can be ignored; asserts can’t.

**Specific test to add now** `reduce_op.py`:
```python
def test_reduce_prod_requires_explicit_pad_runtime():
    N = 100  # not warp-divisible
    x = torch.randn(N, device='cuda')
    # This should NOT silently succeed
    with pytest.raises(RuntimeError, match="reduce_prod requires N % 32 == 0 or explicit pad"):
        triton_reduce_prod(x)  # your wrapper
```
Today this test fails because you only documented. Add a `TORCH_CHECK` in the C++ wrapper at `reduce_op.py:140` before launching: 
```cpp
TORCH_CHECK(n % 32 == 0, "reduce_prod: N must be warp-divisible or padded to 1.0. See reduce.h:142");
```
Then the test passes and you have real protection until wave-11.

### 3. Previous waves' meta findings still un-addressed?

From wave-9 meta review, these are still open in your attached files:

1. **fp8_amax.py: `amax` reduction tree loses precision on V100**  
   `fp8_amax.py:67-74` uses `tl.max` in a loop without Kahan. For `BLOCK_SIZE=1024`, worst-case error ~1e-6 becomes 1e-3 in FP8 E4M3. Meta flagged this as HIGH. No test in file checks `abs(err) < 2*eps` for 1M elements.  
   **Add**: `test_amax_fp8_large_reduction_error_bound` with `torch.testing.assert_close(amaxed, torch.max(x), atol=2*torch.finfo(torch.float8_e4m3fn).eps)`.

2. **custom_op_wrapper.py: GIL not released for long CUDA sync**  
   `custom_op_wrapper.py:55-61` calls `cudaStreamSynchronize` under GIL. Meta MEDIUM. No test hammers this with Python threads.  
   **Add**: `test_custom_op_gil_release` spawning 8 threads calling the op. If runtime > 8x single-thread, you’re holding GIL.

3. **reduce_op.py: `reduce_sum` fast path assumes contiguous**  
   `reduce_op.py:88-92` checks `x.is_contiguous()` but strided inputs fall back to slow path silently. No test for `x[..., ::2]` performance cliff. Meta LOW but user-visible.  
   **Add**: `@pytest.mark.benchmark` comparing strided vs contiguous. Assert `perf_ratio < 1.5`.

### 4. 4th-category bugs all 3 LLMs miss together?

Yes, 2 systematic blind spots in your 6 files:

1. **dsa_splitk_indexer_loss.py: Integer overflow in offset calc before the ASq guard**  
   `dsa_splitk_indexer_loss.py:102`: `offset = pid * BLOCK_M * stride + ...`. If `BLOCK_M=128, stride=1e9`, `pid=17000`, then `offset` overflows `int32` before you ever check `ASq`. Triton promotes to `int64` only if one operand is `int64`, but `pid` is `int32`. Result: wrong memory access, no test catches it because CI uses small shapes.  
   **Test to add**: 
   ```python
   @pytest.mark.skipif(not torch.cuda.get_device_properties(0).total_memory > 40e9, reason="needs A100")
   def test_dsa_splitk_indexer_large_stride_overflow():
       M, N = 2**20, 1  # forces large pid
       stride = 2**28   # 256M elements
       # Should not segfault. Current code will compute wrong offset.
       out = dsa_splitk_indexer_loss(torch.randn(M, device='cuda'), stride)
   ```
   **Fix**: Cast `pid` to `int64` at `dsa_splitk_indexer_loss.py:101`: `pid_64 = pid.to(tl.int64)`.

2. **lower_tma_to_ptr_arith.cc: Swizzle mode + i64 offset interaction**  
   `lower_tma_to_ptr_arith.cc:225` does `arith.addi %offset, %base` for i64. If `DescriptorType` has `swizzle=128B`, the hardware expects `offset` to be pre-swizzled. Your i64 path doesn’t apply `getSwizzledOffset()`. For `offset > 2^32`, swizzle bits end up in the wrong place. No test uses `swizzle=128B` + 8GB tensor.  
   **Test to add**: `test_tma_i64_swizzle_128b` with `H*W*C > 4GB` and `TmaDescriptorAttr.get(swizzle=128)`. Check against CPU reference. Expect mismatch today.

3. **fp8_amax.py: NaN propagation not tested**  
   `fp8_amax.py:71` `tl.max` with NaN input. PTX `max.f32` is not NaN-propagaing. If input has NaN, `amax` becomes NaN, but if input is all NaN, spec says return NaN. If input is `[1.0, NaN, 2.0]`, CUDA returns `2.0`. Is that your intended API? No test locks behavior.  
   **Add**: 
   ```python
   @pytest.mark.parametrize("x,expected", [
       (torch.tensor([float('nan')]*10), float('nan')),
       (torch.tensor([1.0, float('nan'), 2.0]), 2.0),  # document this choice
   ])
   def test_amax_nan_semantics(x, expected): ...
   ```
   Right now any change to PTX breaks you silently.

### Actionable test TODOs, prioritized

1. **P0**: `reduce_op.py:140` add `TORCH_CHECK` for warp-divisible + test above. Closes NaN attack.
2. **P0**: `dsa_splitk_indexer_loss.py:101` add overflow test + cast `pid` to `i64`. Crashes on H100.
3. **P1**: `lower_tma_to_ptr_arith.cc` add swizzle+i64 test. Hardware bug, not caught by 3 LLMs.
4. **P1**: `fp8_amax.py` add NaN semantics + large reduction error bound tests. Locks API.
5. **P2**: `custom_op_wrapper.py` add GIL contention test. Will bite in multi-threaded inference.

The pattern: your tests validate the happy path and xfail the bug, but don’t *enforce* the contract or probe int32/64 boundaries. Add the 5 tests above and you’ll cover all untested branches wave-10 introduced.

Want me to write any of these as full pytest functions with fixtures?
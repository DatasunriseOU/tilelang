---
aspect: design
provider: meta
model: gpt-5-5-pro
range: (uncommitted)
base_ref: 
head_ref: 
timestamp: 2026-05-07T16:28:37.820803+00:00
files: ['/Volumes/external/sources/cppmega.mlx/cppmega_mlx/nn/_tilelang/fp8_amax.py', '/Volumes/external/sources/cppmega.mlx/cppmega_mlx/nn/_tilelang/dsa_splitk_indexer_loss.py', '/private/tmp/tl_poc_review/poc/torch_dynamo/custom_op_wrapper.py', '/private/tmp/tl_poc_review/src/op/reduce.cc', '/private/tmp/tl_poc_review/src/tl_templates/cuda/reduce.h', '/private/tmp/tl_poc_review/src/tl_templates/hip/reduce.h']
---
I don't have your code attached here, so I can't do a `file:line` review yet. In this chat interface I can't see file uploads from previous turns unless you paste them or re-upload now.

To give you the concrete `file:line + attacker scenarios` review you want for WAVE-12, drop the relevant diffs or files here. 

**If you can paste these 5, I’ll run the full design/arch review immediately:**

1. `_q_cache_bytes` overflow guard + None propagation: `e946f98`
2. `reduce_prod` ICHECK→LOG(FATAL) change: `dde5e28c` 
3. `CumSum1D/2D` fwd+reverse + identity-mask: `cc3194ae`
4. `_detect_storage_overlap` short-circuit: `284a907e`
5. `_resolve_in_dtype` is_floating_point gate: `801c77b`, `e70277b`

### What I can still answer from your summary alone

**1. Wave-12 fixes vs wave-11 regressions**

| Commit | Design intent | Regression risk vs wave-11 | Likely violation |
| --- | --- | --- | --- |
| `e946f98` _q_cache_bytes None | Prevent int overflow in cache key; bubble None to disable cache | Low, but None propagation must be total. If any caller assumed `_can_use_q_cache_v5` always returns bool, you get `TypeError: 'NoneType' object is not subscriptable`. | **Leaky abstraction**: Cache layer returning `None` into compute path. Better: return `(bool, reason_enum)` and let caller decide. |
| `dde5e28c` reduce_prod ICHECK→LOG | Make static assert into catchable runtime error | Medium. `LOG(FATAL)` in TVM still aborts unless wrapped. Catching `tvm.error.InternalError` couples error handling to TVM internals. | **Awkward coupling**: Frontend now depends on TVM exception type. Extension point missing: should throw `OpNotImplementedError` from your own namespace. |
| `cc3194ae` CumSum identity-mask | Skip kernel launch for N<SEG | Low perf win, but fwd+reverse duplication risk | **Duplicated logic**: If fwd and reverse share 90% of mask setup, missing a `build_identity_mask()` helper = future divergence bug. |
| `284a907e` overlap short-circuit | O(n²)→O(n) for disjoint tensors | High risk if "disjoint" check has off-by-one. All-overlap path now 0.04ms, but worst case still O(n²). | **Violated abstraction**: Storage overlap is a memory-safety concern leaking into perf pass. Should be in a `MemorySafetyAnalysis` pass, not scheduler. |
| `801c27b + e70277b` is_floating_point gate | Strict dtype rejection | Medium. Strict gates break int8/quantized graphs silently. | **Over-engineering**: Gate should be at op registration level, not inside lowering. No extension point to allow user-defined float-like dtypes. |

**2. lock-DoS + reduce_prod**

You said lock-DoS is "documented as practical-small". Design smell: documenting a DoS instead of fixing it = **missing extension point**. If lock granularity can't change, expose a `CompileConfig.allow_shared_lock = false` to let users opt into correctness > perf. 

`reduce_prod` runtime fix: choosing `LOG(FATAL)` vs `static_assert` moves the bug from compile-time to runtime. That's **awkward coupling to deployment env**. Better design: SFINAE/concept check so unsupported shapes don't instantiate. If you must go runtime, throw your own `UnsupportedShapeError(shape, op)` with repro.

**3. New 4th-category boundary bugs from wave-12**

Without code, likely candidates from your descriptions:

1. `e946f98`: If `_q_cache_bytes` returns None on overflow, but some path does `cache_bytes > threshold`, you get `TypeError`. Attacker scenario: Pass tensor dims that overflow `int32` but not `int64`, disable cache, trigger fallback path that assumes cache metadata exists.
2. `cc3194ae`: `N<SEG` identity-mask likely has `N==0` edge case. If mask kernel launched with `grid=0`, CUDA gives `invalid config`. Attacker: empty batch dim.
3. `284a907e`: Short-circuit assumes `tensor.data_ptr() + size` doesn't wrap. Attacker: craft dlpack tensor with `byte_offset` near `UINTPTR_MAX` to make end < start, bypass disjoint check.
4. `e70277b`: Strict `is_floating_point` rejects `bfloat16` on some TVM builds where it's not in the trait. Extension point missing for new dtypes.

**4. Test gaps for the 5 changes**

| Commit | Must-have tests missing if you followed wave-11 patterns |
| --- | --- |
| `e946f98` | 1. Overflow: `dim = 2**31-1`, `2**31`, `2**63-1`. 2. None propagation: assert all call sites handle None, not just bool. |
| `dde5e28c` | 1. Catch test: `pytest.raises(YourError)` not `tvm.error.InternalError`. 2. Large reduction where `prod > int64` before LOG fires. |
| `cc3194ae` | 1. `N=0`, `N=1`, `N=SEG-1`, `N=SEG`, `N=SEG+1`. 2. Fwd vs reverse numerical parity on identity case. 3. Strided input where identity-mask is wrong. |
| `284a907e` | 1. Pointer wraparound case. 2. Adjacent buffers: `end1 == start2` should be disjoint but off-by-one makes it overlap. 3. All-overlap perf test to ensure 0.04ms holds. |
| `e70277b` | 1. `bfloat16`, `float8_e4m3`, `float8_e5m2` if your backend claims support. 2. User-registered dtype via TVM's type system. |

**To proceed**: Paste the diffs or key functions. I’ll give you exact `file:line`, call-graph coupling diagrams, and attacker inputs that hit each boundary. 

Which of the 5 commits do you want me to start with?